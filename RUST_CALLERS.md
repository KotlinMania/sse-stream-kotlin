# Rust callers — `sse-stream-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `sse-stream` | `sse_stream` | 11 | 2 | 2 | 2 |

## `sse-stream`

### Inbound symbols (5 distinct, 11 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `sse-stream`.

#### `sse_stream::Sse` — 5 site(s) (1 codex, 4 other, 2 caller crates)

- ✅ `codex-kotlin/codex-rmcp-client` `src/http_client_adapter.rs:35` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/common/client_side_sse.rs:9` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/common/reqwest/streamable_http_client.rs:6` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/common/server_side_http.rs:8` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/streamable_http_client.rs:5` — `use sse_stream::Sse`

#### `sse_stream::SseStream` — 2 site(s) (1 codex, 1 other, 2 caller crates)

- ✅ `codex-kotlin/codex-rmcp-client` `src/http_client_adapter.rs:36` — `use sse_stream::SseStream`
- `rmcp-kotlin/rmcp` `src/transport/common/reqwest/streamable_http_client.rs:6` — `use sse_stream::SseStream`

#### `sse_stream::Error` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `rmcp-kotlin/rmcp` `src/transport/common/client_side_sse.rs:9` — `use sse_stream::Error as SseError`
- `rmcp-kotlin/rmcp` `src/transport/streamable_http_client.rs:4` — `pub_use sse_stream::Error as SseError`

#### `sse_stream::KeepAlive` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rmcp-kotlin/rmcp` `src/transport/common/server_side_http.rs:8` — `use sse_stream::KeepAlive`

#### `sse_stream::SseBody` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rmcp-kotlin/rmcp` `src/transport/common/server_side_http.rs:8` — `use sse_stream::SseBody`
