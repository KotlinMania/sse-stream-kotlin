# Port report — `sse-stream-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `sse-stream`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 11 `use` sites from 2 other repos
- **Outbound**: 13 `use` sites to 5 other repos
- **Unresolved external**: 5 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `sse-stream` | `sse_stream` | 11 | 5 | COD-141 | 101 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (11 edges, 2 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `rmcp-kotlin` | `rmcp` | 9 |  |
| `codex-kotlin` | `codex-rmcp-client` | 2 | ✅ |

### Symbol-level demand (5 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `sse_stream::Sse` | 5 | 1 | 4 | 2 |
| `sse_stream::SseStream` | 2 | 1 | 1 | 2 |
| `sse_stream::Error` | 2 | 0 | 2 | 1 |
| `sse_stream::KeepAlive` | 1 | 0 | 1 | 1 |
| `sse_stream::SseBody` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 5 symbols)

##### `sse_stream::Sse` (5 sites, 1 codex, 4 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-rmcp-client` `src/http_client_adapter.rs:35` — `use sse_stream::Sse`

Other workspace call sites:

- `rmcp-kotlin/rmcp` `src/transport/common/client_side_sse.rs:9` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/common/reqwest/streamable_http_client.rs:6` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/common/server_side_http.rs:8` — `use sse_stream::Sse`
- `rmcp-kotlin/rmcp` `src/transport/streamable_http_client.rs:5` — `use sse_stream::Sse`

##### `sse_stream::SseStream` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-rmcp-client` `src/http_client_adapter.rs:36` — `use sse_stream::SseStream`

Other workspace call sites:

- `rmcp-kotlin/rmcp` `src/transport/common/reqwest/streamable_http_client.rs:6` — `use sse_stream::SseStream`

##### `sse_stream::Error` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `rmcp-kotlin/rmcp` `src/transport/common/client_side_sse.rs:9` — `use sse_stream::Error as SseError`
- `rmcp-kotlin/rmcp` `src/transport/streamable_http_client.rs:4` — `pub_use sse_stream::Error as SseError`

##### `sse_stream::KeepAlive` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `rmcp-kotlin/rmcp` `src/transport/common/server_side_http.rs:8` — `use sse_stream::KeepAlive`

##### `sse_stream::SseBody` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `rmcp-kotlin/rmcp` `src/transport/common/server_side_http.rs:8` — `use sse_stream::SseBody`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `bytes-kotlin` | 3 | 2 | `bytes` | COD-26 | 6 | To Do |
| `tokio-kotlin` | 1 | 1 | `tokio` | COD-156 | 10 | To Do |
| `tracing-subscriber-kotlin` | 4 | 2 | `tracing-subscriber` | COD-169 | 28 | To Do |
| `anyhow-kotlin` | 1 | 1 | `anyhow` | COD-10 | 47 | To Do |
| `axum-kotlin` | 4 | 4 | `axum` | COD-23 | 99 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `futures-util` | 183 | 62 | `sse-stream` |
| `hyper` | 64 | 33 | `sse-stream` |
| `hyper-util` | 45 | 15 | `sse-stream` |
| `http-body-util` | 34 | 7 | `sse-stream` |
| `http-body` | 26 | 3 | `sse-stream` |
